class User {
  //acc info
  /*
  String username;
  String email;
  String password;
  */

  //if true, give test day
  //when finish, ask for date to begin
  bool _isNew = true;

  void setNew(bool isNew) {
    _isNew = _isNew;
  }
}
